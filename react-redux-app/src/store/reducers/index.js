const initialState = {
    "ClassJobCategory": {
        "Name": "PLD"
    },
    "Description": "One of the fabled relic weapons.",
    "ID": 1675,
    "LevelItem": 80,
    "Name": "Curtana"
}

export const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}